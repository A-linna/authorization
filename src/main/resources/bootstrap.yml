server:
  port: 8081
  servlet:
    context-path: /account

spring:
  application:
    name: account
  profiles:
    active: local

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.winglibetry.account.db.*